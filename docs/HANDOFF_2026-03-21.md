# Handoff: March 21, 2026

Этот файл нужен как полная точка продолжения работ по `server-vpn` после текущей сессии.

## 1. Что это за проект сейчас

Проект уже не является просто bootstrap-заготовкой. На текущем этапе это рабочий single-node control plane для `Xray-core + VLESS + REALITY`, состоящий из:

- `api`: NestJS + TypeScript + Prisma + PostgreSQL
- `web`: React + TypeScript + Vite
- `xray`: data plane на `443/tcp`
- `caddy`: reverse proxy панели на `8443/tcp`
- host-level `ufw` и `fail2ban`

Архитектурная идея:

- `443` зарезервирован под VPN-трафик Xray
- панель живет на `8443`, потому что домен под публичный TLS для панели пока не задан
- `Xray` и `api` связаны через внутреннюю control network
- PostgreSQL является каноническим control-plane хранилищем
- конфиг Xray не редактируется руками на проде, а рендерится из шаблона

## 2. Что было сделано по этапам

### Этап A. Bootstrap и базовая архитектура

Сделано:

- создана структура monorepo
- подготовлены `ARCHITECTURE.md`, `ROADMAP.md`, `DEPLOY.md`, `SECURITY.md`, `CHANGELOG.md`
- собраны `docker-compose.yml`, `.env.example`, линтинг, форматирование, базовая CI-структура
- выбрана схема `VLESS + REALITY + XTLS Vision` как стартовый production-профиль

Почему так:

- хорошая совместимость с современными VLESS/Xray-клиентами
- минимальная лишняя сложность для single-VPS
- понятная модель дальнейшего роста

### Этап B. Backend core

Сделано:

- безопасный admin auth flow
- bcrypt hashing
- access token + refresh flow
- refresh session storage в PostgreSQL
- bootstrap первого администратора
- brute-force protection
- audit log
- clients CRUD
- expiry logic
- traffic reset
- subscription generation
- config rendering для клиента

Ключевые файлы:

- [auth.service.ts](/D:/server-VPN/apps/api/src/modules/auth/auth.service.ts)
- [clients.service.ts](/D:/server-VPN/apps/api/src/modules/clients/clients.service.ts)
- [subscriptions.service.ts](/D:/server-VPN/apps/api/src/modules/subscriptions/subscriptions.service.ts)
- [audit-log.service.ts](/D:/server-VPN/apps/api/src/modules/audit-log/audit-log.service.ts)

### Этап C. Xray runtime integration

Сделано:

- vendored proto files для работы с Xray control API
- gRPC integration из backend в Xray
- sync клиентов в runtime
- reconcile после рестартов
- periodic traffic snapshots в PostgreSQL
- quota blocking
- system/runtime status summary

Ключевые файлы:

- [xray.service.ts](/D:/server-VPN/apps/api/src/modules/xray/xray.service.ts)
- [system.service.ts](/D:/server-VPN/apps/api/src/modules/system/system.service.ts)
- [dashboard.service.ts](/D:/server-VPN/apps/api/src/modules/dashboard/dashboard.service.ts)

### Этап D. Frontend admin panel

Сделано:

- русифицированная панель
- login flow
- dashboard
- clients list
- client details
- edit forms
- QR modal
- subscription/config UX
- server status page
- admin users
- audit feed

Потом дополнительно были добавлены:

- import/export клиентов
- backups page
- logs page

Ключевые файлы:

- [clients-page.tsx](/D:/server-VPN/apps/web/src/features/clients/clients-page.tsx)
- [dashboard-page.tsx](/D:/server-VPN/apps/web/src/features/dashboard/dashboard-page.tsx)
- [server-status-page.tsx](/D:/server-VPN/apps/web/src/features/system/server-status-page.tsx)
- [backups-page.tsx](/D:/server-VPN/apps/web/src/features/backups/backups-page.tsx)
- [logs-page.tsx](/D:/server-VPN/apps/web/src/features/logs/logs-page.tsx)

### Этап E. Backups, logs, import/export

Сделано:

- file-based logging для API, Xray и Caddy
- API endpoints для `backups`
- UI для `backups`
- API endpoints для `logs`
- UI tail viewer для `logs`
- export клиентов в JSON
- import клиентов из JSON

### Этап F. RBAC и стабилизация доступа (2026-03-31)

Сделано:

- добавлена модель ролей (`SUPER_ADMIN`, `OPERATOR`, `READ_ONLY`) через общий `RolesGuard`;
- на API слой повесили `@Roles` на основные контроллеры:
  - `clients`, `admin-users`, `backups`, `dashboard`, `logs`, `system`, `audit-log`, `xray`;
- запретил модификации для `READ_ONLY` на уровне UI:
  - клиенты: создание/импорт/удаление/редактирование/переключение/продление/ресет;
  - бекапы: создание/restore/delete/вызов модального восстановления;
  - чтение и экспорт логов/архивов остаётся доступным;
- исправил `/api/logs/:sourceId` на потоковый хвост (без чтения всего файла в память);
- убран конфликтовый браузерный popup basic auth в Caddy для UI-панели;
- проверил дефолтные параметры авто-резервного копирования:
  - интервал авто-создания уже стоит на 5 дней,
  - очистка по retention — 14 дней.

Осталось:

- завершить финальную верификацию в браузере после деплоя (особенно поведение read-only и fallback-режимов);
- синхронизировать public-репозиторий с обновлениями после отдельного косметического прохода README/док.

Ключевые файлы:

- [file-console.logger.ts](/D:/server-VPN/apps/api/src/common/logging/file-console.logger.ts)
- [backups.service.ts](/D:/server-VPN/apps/api/src/modules/backups/backups.service.ts)
- [logs.service.ts](/D:/server-VPN/apps/api/src/modules/logs/logs.service.ts)
- [client-import-export.schema.ts](/D:/server-VPN/apps/api/src/modules/clients/client-import-export.schema.ts)

## 3. Что было сделано на VPS

Сервер:

- IP: `45.66.11.243`
- deploy path: `/opt/server-vpn`

Хост подготовлен:

- установлен Docker
- установлен `docker compose`
- настроен `ufw`
- настроен `fail2ban`
- развернут проект в `/opt/server-vpn`

Runtime/secret paths на сервере:

- `/opt/server-vpn/.env`
- `/opt/server-vpn/infra/xray/generated/config.json`
- `/opt/server-vpn/infra/runtime/logs`
- `/opt/server-vpn/infra/backup/output`
- `/root/server-vpn-secrets/admin-bootstrap.txt`
- `/root/server-vpn-secrets/panel-basic-auth.txt`
- `/root/.ssh/server-vpn-deploy`

Важно:

- значения секретов в git не коммитились
- реальные ключи и пароли есть только на сервере

## 4. Важные production-fixes, которые были нужны по дороге

### 4.1. Xray protobuf/runtime fixes

Были исправлены проблемы с runtime protobuf paths и gRPC invocation:

- `94f5339` `fix: resolve xray protobuf paths in runtime image`
- `24170b0` `fix: bind xray grpc client calls correctly`

### 4.2. Script invocation fix

- `a60031a` `fix: invoke xray render script via bash`

### 4.3. Backups/logs/import-export feature slice

- `90f8617` `feat: add backups logs and client transfer tools`

### 4.4. Xray log permission fix

После включения file logs Xray падал из-за permission denied на host bind mount.

Исправление:

- `c1fbbd6` `fix: prepare runtime log files before deploy`

### 4.5. Basic auth vs Bearer conflict

Был важный баг: edge `basic_auth` в Caddy стоял на `/api/*`, а frontend использует `Bearer` для app auth. Это делало браузерную панель логически сломанной.

Исправление:

- `aa75c47` `fix: bypass edge basic auth for api routes`

Итоговая модель:

- UI root и статика по-прежнему защищены basic auth
- `/api/*` идет без edge basic auth
- API защищен приложением: login + bearer + refresh flow

### 4.6. Legacy BACKUP_DIR migration

На сервере был старый `BACKUP_DIR` с host path вместо container path, из-за чего backup creation отдавал `500`.

Исправление:

- `51fe1ab` `fix: normalize legacy backup path in deploy`

`deploy.sh` теперь умеет автоматически нормализовать legacy `BACKUP_DIR` в `.env`.

## 5. Что проверено руками и реально работает

Проверено на production-хосте 2026-03-21:

- `docker compose ps`: `api`, `postgres`, `caddy`, `xray` были `healthy`
- root UI без basic auth возвращает `401`
- `/api/auth/login` возвращает `201`
- `/api/backups`:
  - создание работает
  - список работает
  - download route работает
- `/api/logs/sources` работает
- `/api/logs/api?lines=20` работает
- `/api/xray/profiles` отвечает и показывает `runtime.status = healthy`
- `/api/clients/export` работает

Во время smoke test были созданы реальные backup-архивы. Поэтому на сервере уже есть несколько test backups.

На момент последней проверки:

- клиентов в базе: `0`
- backup snapshots в базе были уже не пустыми из-за smoke tests

## 6. Что осталось недоделанным

Не реализовано или реализовано не до конца:

- restore flow из UI
- 2FA для админов
- настоящий runtime enforcement для `deviceLimit` и `ipLimit`
- более глубокие графики и аналитика по дням
- import/export полной истории daily usage, а не только агрегатов
- safe maintenance actions из UI вроде controlled service restart
- более развитый logs UX: фильтры, поиск, уровни, пагинация

## 7. Что важно помнить перед продолжением

### Секреты

Нельзя коммитить:

- `.env` с реальными значениями
- REALITY private key
- admin/bootstrap пароли
- panel basic auth credentials
- рабочие subscription URLs

### Прод деплой

Базовая последовательность:

1. локально: `npm run lint && npm run typecheck && npm run test && npm run build`
2. `git push origin main`
3. на сервере: `cd /opt/server-vpn && bash infra/scripts/deploy.sh`

### Restore

Restore сейчас считается осознанной host-side операцией:

`./infra/scripts/restore.sh --yes-restore /absolute/path/to/archive.tar.gz`

### Где продолжать код

Если продолжать с backend/core:

- [clients.service.ts](/D:/server-VPN/apps/api/src/modules/clients/clients.service.ts)
- [xray.service.ts](/D:/server-VPN/apps/api/src/modules/xray/xray.service.ts)
- [backups.service.ts](/D:/server-VPN/apps/api/src/modules/backups/backups.service.ts)
- [logs.service.ts](/D:/server-VPN/apps/api/src/modules/logs/logs.service.ts)

Если продолжать с UI:

- [clients-page.tsx](/D:/server-VPN/apps/web/src/features/clients/clients-page.tsx)
- [backups-page.tsx](/D:/server-VPN/apps/web/src/features/backups/backups-page.tsx)
- [logs-page.tsx](/D:/server-VPN/apps/web/src/features/logs/logs-page.tsx)
- [auth-context.tsx](/D:/server-VPN/apps/web/src/features/auth/auth-context.tsx)

Если продолжать с infra/security:

- [docker-compose.yml](/D:/server-VPN/docker-compose.yml)
- [Caddyfile](/D:/server-VPN/infra/caddy/Caddyfile)
- [deploy.sh](/D:/server-VPN/infra/scripts/deploy.sh)
- [bootstrap-server.sh](/D:/server-VPN/infra/scripts/bootstrap-server.sh)

## 8. Самый логичный следующий этап

Я бы продолжал в таком порядке:

1. `2FA` для админов
2. `restore` UX и безопасный recovery flow
3. enforcement для `deviceLimit` / `ipLimit`
4. расширенная аналитика и графики
5. maintenance operations с audit trail

## 9. Последние ключевые коммиты

- `51fe1ab` `fix: normalize legacy backup path in deploy`
- `aa75c47` `fix: bypass edge basic auth for api routes`
- `c1fbbd6` `fix: prepare runtime log files before deploy`
- `90f8617` `feat: add backups logs and client transfer tools`
- `24170b0` `fix: bind xray grpc client calls correctly`
- `94f5339` `fix: resolve xray protobuf paths in runtime image`
- `a60031a` `fix: invoke xray render script via bash`
- `5945b93` `feat: integrate xray runtime sync and client config ux`
- `ba6a723` `feat: wire auth clients and localized admin panel`

## 10. Короткий итог

Сейчас проект находится в состоянии первого рабочего self-hosted релиза, который:

- поднимается на одном VPS
- имеет живой Xray runtime
- имеет рабочую веб-панель
- умеет auth, clients CRUD, subscriptions, stats, logs, backups, import/export
- не хранит секреты в git

Это уже хорошая база для следующей итерации, а не черновик с макетами.

## 11. Update: March 24, 2026

### Что сделано сегодня

За текущую сессию были закрыты следующие задачи:

- исправлен баг выбора клиента в UI, при котором карточка деталей "прыгала" между двумя клиентами
- переработан визуал панели:
  - dropdown/select элементы приведены к общей теме
  - убраны непрофессиональные маркетинговые подписи вроде "панель управления для одного VPS"
  - поправлен header/mobile navigation, чтобы в сложенном состоянии не перекрывать весь экран
- улучшены страницы `Clients`, `Logs`, `Backups`, `Admin Users`
- добавлена отдельная вкладка помощи с краткими инструкциями по использованию панели и рекомендациями по клиентам под платформы
- в backups добавлено удаление backup-архивов из UI
- реализована полноценная admin `TOTP 2FA`:
  - backend login flow теперь умеет second-factor challenge
  - секреты 2FA хранятся в зашифрованном виде
  - добавлены API routes для setup / enable / disable 2FA
  - в UI появился setup flow с QR и подтверждением кодом
  - login page поддерживает второй шаг с вводом 6-digit TOTP

### Коммиты за сегодня

- `9387915` `fix: stabilize client selection in clients panel`
- `89073ab` `feat: polish panel ui and backup workflows`
- `8031b7d` `feat: add admin totp two-factor auth`
- `374eef9` `fix: narrow 2fa login session payload type`
- `7730b90` `fix: narrow auth login response by access token`
- `5f49688` `fix: harden totp digest indexing`

### Что было задеплоено

Прод и `origin/main` синхронизированы на:

- `5f49688` `fix: harden totp digest indexing`

Подтверждено на VPS:

- `docker compose` после deploy поднял `api`, `postgres`, `xray`, `caddy`
- `api` стал `healthy`
- `/healthz` внутри контейнера отвечает `ok`
- `/readyz` внутри контейнера отвечает `ready`
- новые routes 2FA зарегистрированы в Nest runtime:
  - `/api/admin-users/me/two-factor`
  - `/api/admin-users/me/two-factor/setup`
  - `/api/admin-users/me/two-factor/enable`
  - `/api/admin-users/me/two-factor/disable`

### Что осталось незавершенным после этой сессии

Следующие пункты по-прежнему остаются в работе:

- restore flow из UI и безопасный recovery flow поверх `infra/scripts/restore.sh`
- настоящий runtime enforcement для `deviceLimit` и `ipLimit`
- более глубокая аналитика и дневные графики
- logs UX все еще MVP-уровня, хоть и стал лучше

### На чем остановились прямо сейчас

После завершения 2FA следующим активным пунктом был выбран:

- `restore` UX и safe restore flow

Перед остановкой была уже подтверждена готовность прод-состояния после 2FA deploy, и следующим шагом должен быть разбор текущего backup/restore flow в:

- `apps/api/src/modules/backups`
- `apps/web/src/features/backups`
- `infra/scripts/restore.sh`

### Внешние материалы для последующего разбора

Нужно дополнительно вернуться к внешнему shared-chat материалу и сверить его выводы с текущим состоянием проекта:

- ChatGPT shared link: `https://chatgpt.com/s/cd_69c2dfe33ef8819186ac4152c9974d23`
- заголовок страницы при проверке 2026-03-24: `Run code analysis without modifications`
- метка на странице: `Mar 24 · Twoia-Kotletochka/server-vpn · main`

Что сделать позже:

- открыть этот shared chat вручную
- выписать из него выводы / замечания / найденные риски
- сравнить их с уже внедренными изменениями от 2026-03-24
- отдельно проверить, не закрыта ли часть замечаний коммитами `9387915`, `89073ab`, `8031b7d`, `374eef9`, `7730b90`, `5f49688`, `ce49d8a`

Важно:

- автоматическое чтение содержимого shared page сейчас дало только title/date/repo marker, без полного текста разговора
- поэтому ссылка сохранена как артефакт на последующий ручной анализ, а не как уже разобранный источник

## 12. Update: March 26, 2026

### Что сделано сегодня

За текущую сессию был закрыт следующий блок работ:

- реализован safe `restore` preflight flow без destructive запуска из UI:
  - backend теперь умеет читать manifest из backup-архива, проверять наличие postgres dump и xray config, сравнивать SHA-256 и строить restore preflight
  - добавлен API endpoint `GET /api/backups/:backupId/restore-plan`
  - в UI backups появился отдельный restore plan с явными командами `--dry-run` и `--yes-restore`, статусом preflight и предупреждениями
  - host-side `infra/scripts/restore.sh` получил `--dry-run`, валидацию архива, защиту по абсолютному пути и более безопасную логику работы с runtime config Xray
- разобраны замечания code review из shared-chat и прогнаны серверные проверки:
  - typecheck-ошибки по Prisma оказались следствием отсутствующего `prisma generate`
  - один API test падал из-за отсутствующего `TOTP_ENCRYPTION_SECRET` в test env
  - два web test были реально сломаны и исправлены
  - после этого в чистом containerized workspace стали зелёными `typecheck`, `test`, `build`
- в процессе деплоя всплыл production bug в Docker build API:
  - runtime image стартовал без `apps/api/dist/main.js`
  - причина: stale `*.tsbuildinfo` в Docker context и инкрементальная сборка Nest/TypeScript
  - исправлено через исключение `*.tsbuildinfo` из `.dockerignore`, очистку build-info перед `npm run build -w apps/api` и явную проверку `test -f /app/apps/api/dist/main.js` прямо в Dockerfile
- после hotfix прод снова поднят и проверен
- доведён runtime enforcement для `deviceLimit` / `ipLimit`:
  - `XrayService` теперь при usage snapshot читает online-IP telemetry из Xray Stats API
  - `activeConnections` в текущем MVP теперь отражает число одновременных online endpoint, а не просто `0/1`
  - при превышении лимитов клиент автоматически переводится в `BLOCKED`, удаляется из live Xray runtime и получает audit trail с причиной
  - для совместимости добавлен fallback с `GetStatsOnlineIpList` на `GetStatsOnline`
  - в help-разделе добавлена явная подсказка, как именно эти лимиты трактуются в текущем runtime
- исправлен раздражающий browser `Basic Auth` popup на панели:
  - причиной был запрос браузера к `/favicon.ico`, который раньше тоже попадал под `basic_auth` и мог повторно вызывать challenge
  - в Caddy вынесены `favicon/manifest` asset paths из `basic_auth`
  - добавлен `favicon.svg`, а `/favicon.ico` теперь переписывается в него и отвечает без `401`

### Коммиты за сегодня

- `f4be251` `feat: add safe backup restore preflight`
- `2da132f` `fix: tighten backup restore test types`
- `0ac53d9` `test: cover totp env requirement`
- `559bf84` `test: stabilize web test harness`
- `ade9377` `test: narrow client selection assertions`
- `c9a4f34` `fix: force clean api docker build outputs`
- `27cf263` `feat: enforce client runtime access limits`
- `79b90c3` `fix: format runtime limit enforcement changes`
- `aa71bcc` `fix: avoid basic auth prompt on panel assets`

### Что показал разбор замечаний обзорщика

Из внешнего review material и повторных проверок подтвердилось следующее:

- `lint` действительно оставался красным, но краснота на текущий момент — это не runtime regression, а `Biome` format/import-order debt
- массовые ошибки typecheck по Prisma не были новым дефектом бизнес-логики; они исчезают после `npm run prisma:generate -w apps/api`
- часть test failures была инфраструктурной:
  - отсутствующий `TOTP_ENCRYPTION_SECRET`
  - некорректный harness для `ClientsPage`
  - неоднозначный matcher в `BackupsPage`

Итог после исправлений:

- `typecheck`: green
- `test`: green
- `build`: green
- `lint`: всё ещё red, сейчас это 12 ошибок формата / organizeImports в нескольких старых файлах

### Что было задеплоено

Прод и `origin/main` синхронизированы на:

- `79b90c3` `fix: format runtime limit enforcement changes`

Подтверждено на VPS после deploy:

- `server-vpn-api`, `server-vpn-caddy`, `postgres`, `xray` в состоянии `healthy/running`
- внешний `GET /healthz`: `20/20 -> 200`
- внешний `GET /readyz`: `20/20 -> 200`
- авторизованный `GET /api/backups`: `20/20 -> 200`
- авторизованный `GET /api/backups/:id/restore-plan`: `10/10 -> 200`
- login через реальный host `https://45.66.11.243:8443/api/auth/login` возвращает `201` и `accessToken`
- после deploy runtime-limit slice дополнительно проверен через публичный контур:
  - `GET /healthz`: `10/10 -> 200`
  - `GET /readyz`: `10/10 -> 200`
  - panel root с `Basic Auth`: `200`
  - login: `201`
  - авторизованный `GET /api/clients?page=1&pageSize=10`: `5/5 -> 200`
  - авторизованный `GET /api/backups`: `5/5 -> 200`
  - авторизованный `GET /api/system/status`: `5/5 -> 200`
  - авторизованный `GET /api/xray/profiles`: `5/5 -> 200`
  - на момент проверки в базе было `7` клиентов, `2` backup-записи, `runtime.status = healthy`, `onlineUsers = 0`
- после panel-auth fix дополнительно подтверждено:
  - `GET /favicon.ico` без auth: `200`
  - `GET /favicon.svg` без auth: `200`
  - panel root с `Basic Auth`: `200`
  - panel root без auth остаётся закрытым: `401`

Практический результат по restore flow:

- список backups возвращает обе команды:
  - `./infra/scripts/restore.sh --dry-run --yes-restore /absolute/path/to/archive.tar.gz`
  - `./infra/scripts/restore.sh --yes-restore /absolute/path/to/archive.tar.gz`
- первый доступный backup на сервере:
  - `server-vpn-20260325-172954Z.tar.gz`
- для него preflight сейчас:
  - `canRestore = true`
  - `warnings = 0`
  - dry-run команда формируется с точным путём до архива

### Нюанс, который важно помнить

- запросы к Caddy на `https://127.0.0.1:8443/...` без корректного `Host` не отражают реальное поведение панели
- для корректной проверки внешнего auth/login нужен host `45.66.11.243:8443` (или `curl --resolve ...`)
- реальный внешний контур после deploy работает корректно
- в текущем Xray-based MVP и `deviceLimit`, и `ipLimit` опираются на число одновременных online endpoint / online IP, потому что runtime даёт именно эту телеметрию, а не устойчивый device fingerprint

### Что осталось незавершенным после этой сессии

Следующие пункты остаются следующими по приоритету:

- более глубокая аналитика и графики
- дальнейшее улучшение logs UX
- cleanup текущего `lint` debt (`Biome` format/import-order)

### На чем остановились прямо сейчас

Текущий safe restore slice уже в проде и smoke-tested.

Следующий разумный продуктовый шаг:

1. enforcement для `deviceLimit` / `ipLimit`
2. аналитика и usage dashboards
3. logs UX cleanup
4. отдельно пройтись по `lint` и убрать оставшийся formatting debt

## Update 2026-03-26: Automatic backup policy

### Что изменили

- Добавлен встроенный scheduler в `BackupsService`: API теперь сам периодически проверяет backup policy и при необходимости создаёт новый backup без ручного действия из UI.
- Политика вынесена в env:
  - `BACKUP_AUTO_CREATE_ENABLED=true`
  - `BACKUP_AUTO_CREATE_INTERVAL_DAYS=5`
  - `BACKUP_AUTO_MAINTENANCE_INTERVAL_MS=3600000`
  - `BACKUP_RETENTION_DAYS=14`
- Автобэкап использует тот же безопасный pipeline, что и ручной backup:
  - общий `createSnapshot(...)`
  - тот же tar/archive flow
  - тот же checksum / manifest
  - тот же `pruneExpiredBackups()`
- В `GET /api/backups` policy теперь явно возвращает:
  - `autoCreateEnabled`
  - `autoCreateIntervalDays`
  - `retentionDays`
- В вебе на странице backups добавлено отображение policy: видно, что автобэкап включён и создаётся каждые 5 дней.

### Как это работает

- При старте API через ~15 секунд выполняется bootstrap-check policy.
- Дальше scheduler крутится раз в час и решает, пора ли создавать новый архив.
- Новый backup создаётся только если:
  - автобэкап не выключен
  - нет свежего `CREATING` snapshot, чтобы не словить дубль во время гонки
  - с момента последнего успешного backup прошло не меньше `5` дней
- Если backup ещё не пора создавать, maintenance всё равно делает cleanup по retention policy.
- Cleanup остаётся безопасным: старые архивы старше `14` дней помечаются как `PRUNED` и удаляются с диска через уже существующий prune flow.

### Что важно по безопасности

- Existing backup/restore flow не менялся в destructive сторону:
  - restore-команды прежние
  - формат архива прежний
  - старые архивы и конфиги не мигрировались и не переписывались
- Ретеншн применяется по age, а не по fixed-count. То есть удаляется не просто "самый старый", а любой backup старше 14 дней.
- На момент rollout последний успешный backup был `2026-03-25`, поэтому scheduler не должен был создавать дубликат сразу после deploy.

### Что проверено

Containerized validation на VPS:

- `npm run typecheck`: green
- `npm run test`: green
- `npm run build`: green
- `npm run lint`: всё ещё red, но только на старом `Biome` debt, без новых критичных дефектов этого slice

После deploy на прод:

- `origin/main`, локальный repo и `/opt/server-vpn` синхронизированы на `74a81a8`
- `GET https://45.66.11.243:8443/healthz`: `200`
- `GET https://45.66.11.243:8443/readyz`: `200`
- login через `/api/auth/login`: `201`
- `GET /api/backups` с `Bearer` подтверждает policy:
  - `autoCreateEnabled = true`
  - `autoCreateIntervalDays = 5`
  - `retentionDays = 14`
- На сервере в момент проверки уже были доступны как минимум два валидных backup-архива:
  - `server-vpn-20260325-172954Z.tar.gz`
  - `server-vpn-20260320-222210Z.tar.gz`

### На чем остановились после этого

Автоматическое создание backup и cleanup по retention уже в проде.

Следующие продуктовые шаги после этой итерации:

1. аналитика и usage dashboards
2. logs UX cleanup
3. cleanup текущего `lint` debt

## Update 2026-03-26: Dashboard live metrics clarified

### Что исправили

- На dashboard убрана путаница между клиентами со статусом `ACTIVE` и теми, кто реально подключён к VPN прямо сейчас.
- Первая карточка теперь показывает `Онлайн сейчас`:
  - это live-число подключённых пользователей из `Xray runtime`
- Отдельно выведена карточка `Доступные клиенты`:
  - это клиенты со статусом `ACTIVE`, то есть разрешённые к подключению, но не обязательно онлайн в данный момент
- В блоке `Текущая сводка` та же семантика повторена текстом:
  - `Доступных клиентов`
  - `Клиентов онлайн сейчас`

### Что изменили в коде

- `DashboardService` теперь возвращает в `totals` оба значения:
  - `available`
  - `onlineNow`
- `active` пока сохранён как совместимый alias значения `available`, чтобы не ломать внутренний контракт без необходимости
- Веб использует новые поля и больше не подписывает бизнес-статус как live-активность

### Что проверено

Containerized validation на VPS:

- `typecheck`: green
- `test`: green
  - API: `29/29`
  - Web: `5/5`
  - добавлены отдельные тесты на `DashboardService` и `DashboardPage`
- `build`: green
- `lint`: red только на старом хвосте в других файлах; новые dashboard-файлы этот хвост не увеличили

### Практический результат

- На проде dashboard теперь должен отвечать на два разных вопроса:
  - сколько клиентов вообще доступны к подключению
  - сколько из них онлайн прямо сейчас
- Это снимает ложное ощущение, что все `ACTIVE` клиенты уже сидят в VPN-туннеле в данный момент

## Update 2026-03-26: Operator admin accounts

### Что реализовали

- В панель добавлено управление подчинёнными операторскими аккаунтами.
- `SUPER_ADMIN` теперь может:
  - создавать новые аккаунты уровня `OPERATOR`
  - удалять операторские аккаунты из панели
- `OPERATOR` теперь намеренно отделён от super-admin контура:
  - не может создавать админов
  - не может удалять админов
  - не может удалить даже самого себя через admin-management endpoint
- При этом `OPERATOR` сохраняет полный текущий рабочий контур по клиентам:
  - создание клиентов
  - изменение клиентов
  - удаление клиентов

### Ограничения и правила безопасности

- Через этот новый UI/API нельзя создавать дополнительных `SUPER_ADMIN`.
- Через панель нельзя удалить `SUPER_ADMIN`.
- Текущий `SUPER_ADMIN` не может удалить сам себя.
- Роль `READ_ONLY` в схеме всё ещё существует как будущий задел, но в текущем продукте через UI/API новые аккаунты создаются только как `OPERATOR`.

### Что изменили в коде

- Backend:
  - `POST /api/admin-users` для создания `OPERATOR`
  - `DELETE /api/admin-users/:adminUserId` для удаления подчинённого аккаунта
  - role gating в `AdminUsersService`
  - capability flags в `GET /api/admin-users`
- Frontend:
  - форма создания оператора в разделе `Администраторы`
  - delete action только там, где backend разрешает `canDelete`
  - скрытие operator-management controls для несупер-админов

### Что проверено

Containerized validation на VPS:

- `typecheck`: green
- `test`: green
  - API: `35/35`
  - Web: `7/7`
- `build`: green
- `lint`: red только на старом несвязанном хвосте; новые admin/operator файлы этот хвост не увеличили

Live smoke на проде через реальный API:

- `SUPER_ADMIN` создал временный `OPERATOR`
- новый `OPERATOR` успешно вошёл
- `OPERATOR` успешно создал временного клиента
- `OPERATOR` успешно удалил этого же клиента
- попытка `OPERATOR` удалить admin-account вернула `403`
  - сообщение: `Only super admins can manage admin accounts.`
- `SUPER_ADMIN` после этого удалил временного оператора

### На чем остановились после этого

- Базовый multi-admin flow для панели уже рабочий:
  - super-admin управляет составом операторов
  - операторы работают с клиентами
- Следующие логичные шаги после этого:
  1. при необходимости ввести отдельную реальную `READ_ONLY` permission model
  2. углубить аналитику и usage dashboards
  3. продолжить logs UX cleanup

## Update 2026-03-26: README wording and transport clarification

### Что уточнили

- В `README` обновлено краткое позиционирование проекта на русском и английском.
- Формулировка приведена в соответствие с реальной конфигурацией MVP:
  - это не просто `VLESS`
  - текущий рабочий transport profile именно `VLESS + REALITY`

### Почему это важно

- В Xray-шаблоне inbound поднят как:
  - `protocol: vless`
  - `streamSettings.security: reality`
- Генерируемые клиентские ссылки тоже явно содержат:
  - `security=reality`
  - `flow=xtls-rprx-vision`
- То есть:
  - `Xray-core` здесь выступает runtime-движком
  - `VLESS` это протокол клиента/inbound
  - `REALITY` это transport security / camouflage layer поверх этого VLESS-контура

### Практический вывод

- Корректное краткое описание текущего продукта:
  - control plane для `Xray-core`
  - single-VPS deployment
  - transport в MVP: `VLESS + REALITY`
  - вокруг этого построены subscriptions, client management, backups и admin panel

## Update 2026-03-26: public repository preparation

### Что сделали

- Подготовили public-ready слой документации для нового репозитория `xray-vpn-control-plane`.
- Полностью переписали верхний `README` в более продуктовый и профессиональный вид.
- Добавили двуязычную документацию:
  - английские root docs остались основными
  - русские зеркала добавлены в `docs/ru/*`
- Привели `DEPLOY.md`, `ROADMAP.md`, `SECURITY.md`, `ADMIN_GUIDE.md`, `USER_GUIDE.md`, `TROUBLESHOOTING.md` в соответствие с реальным текущим состоянием проекта.

### Что специально обезличили для публичного репозитория

- В `.env.example` убраны server-specific значения:
  - больше нет жёстко зашитого IP `45.66.11.243`
  - panel/subscription URLs заменены на `panel.example.com`
  - basic auth username сделан нейтральным (`operator`)
- Из public-export контура исключены внутренние operational artefacts:
  - `docs/HANDOFF_*.md`
  - `.tmp/`
  - runtime dirs
  - generated config/runtime files
  - `secrets/`

### Что добавили для будущих публикаций

- `.public-export-ignore` как единый список исключений для публичного mirror/export.
- `infra/scripts/export-public-repo.sh` для сборки чистого snapshot в отдельный working tree публичного репозитория.
- GitHub Actions workflow обновлён так, чтобы реагировать и на `main`, а не только на legacy `master`.

### Зачем это важно

- Дальнейшие публикации в новый публичный репозиторий можно делать повторяемо и без ручного вычищения handoff/операционных файлов.
- Публичная документация теперь уже описывает реальный текущий продукт, а не только ранний MVP-скелет.

## Update 2026-03-26: repeated browser basic auth popup fix

### Симптом

- После входа в web panel браузер всё ещё периодически поднимал системный `Basic Auth` popup.
- На проде это воспроизводилось у реального пользователя уже после входа в приложение.

### Причина

- `Caddy` всё ещё защищал `Basic Auth`-ом runtime static assets панели.
- В access log это проявлялось как `401` на:
  - `/assets/*.woff`
  - `/assets/*.woff2`
  - и другие файлы из `Vite dist`
- Такие `401` на фоне провоцировали browser-native auth prompt, хотя проблема была уже не во внутренней admin-сессии приложения.

### Что изменили

- В [infra/caddy/Caddyfile](/Users/pc/server-VPN/infra/caddy/Caddyfile) `@public_panel_assets` расширен на `/assets/*`.
- В итоге:
  - HTML routes панели по-прежнему остаются за `Basic Auth`
  - `API` по-прежнему защищён приложением и исключён из edge basic auth
  - static runtime assets больше не отдают `401`

### Что проверили

- `GET /` без auth -> `401`
- `GET /assets/dm-sans-latin-700-normal-DvUfVpUG.woff2` без auth -> `200`
- `GET /dashboard` с корректным basic auth -> `200`
- `GET /healthz` -> `200`

### Итог

- Системный popup больше не должен выскакивать из-за фоновой загрузки шрифтов и других assets.
- Если браузер уже закэшировал старый challenge, пользователю может понадобиться полный reload вкладки один раз после деплоя.

## Update 2026-03-26: public repository publishing and GitHub presentation

### Что сделали

- Опубликовали отдельный public repository: `Twoia-Kotletochka/xray-vpn-control-plane`.
- Перенесли в него уже подготовленный public-ready snapshot проекта без operational artefacts и секретов.
- Усилили presentation слой репозитория внутри GitHub:
  - обновили repository description
  - включили `Discussions`
  - выставили topics под домен и стек
  - подготовили первый публичный release
  - открыли стартовые issues под roadmap/docs

### Что добавили в public repo

- Community files:
  - `LICENSE`
  - `CONTRIBUTING.md`
  - `CODE_OF_CONDUCT.md`
  - `SUPPORT.md`
- GitHub templates:
  - issue templates для bug/feature/docs
  - PR template
  - release notes config
- Улучшили верхний `README`, quick start, bilingual positioning и карту документации уже именно в опубликованном public repo.

### Проверка и итог

- Public repository после оформления начал выглядеть как самостоятельный продуктовый open/public проект, а не просто mirror исходников.
- GitHub community profile был доведён до `100%`.
- Первый публичный tag/release подготовлен как стартовая точка для дальнейших внешних публикаций.

## Update 2026-03-26: social preview fix for public repo

### Проблема

- Сгенерированные social preview assets для public repo были визуально испорчены:
  - PNG оказался в неправильной геометрии
  - в preview появлялось большое пустое белое поле
  - исходный layout был перегружен

### Что изменили

- Пересобрали social preview assets публичного репозитория:
  - `docs/assets/social-preview.svg`
  - `docs/assets/social-preview.png`
- Нормализовали итоговый PNG под целевой размер `1280x640`.
- Упростили композицию, чтобы картинка адекватно читалась как Open Graph / GitHub preview.

### Практический вывод

- Для загрузки в `GitHub -> Settings -> Social preview` теперь пригоден именно новый `social-preview.png`.
- Сам public repo получил нормальную share-card обложку без артефактов.

## Update 2026-03-27: panel screenshot added to public README

### Что сделали

- Сняли реальный screenshot уже авторизованной web panel для публичного README.
- В качестве безопасного кадра выбрана страница `Help`, чтобы показать shell панели, sidebar, topbar и общий UI, не засветив реальные клиентские данные/операционную статистику.
- Итоговый asset сохранён в public repo как `docs/assets/panel-overview.png`.
- Screenshot встроен прямо в верхнюю часть `README`, чтобы внешний пользователь видел внешний вид продукта сразу на первом экране репозитория.

### Как снимали

- Использовали headless browser с реальным входом в панель, а не mock/static картинку.
- Для стабильности кадр делался после честного UI login flow, затем через SPA routing открывалась `Help` page и снимался готовый shell.

### Итог

- Public repository теперь показывает не только описание проекта, но и реальный внешний вид панели.
- Это усиливает доверие и конверсию в просмотр README / stars / forks при первом знакомстве с репозиторием.

## Update 2026-03-31: final local pass before VPS rollout

### Что сделали

- Исправили наименования метрик на дашборде:
  - `available clients` переименованы в `Active profiles` / `Активные профили`.
  - Это убирает ложное ощущение, что метрика означает «только онлайн», т.к. она отражает статус `ACTIVE`.
- Переставили порядок метрик в карточках на дашборде: сначала статусные блоки, затем онлайн-подключения.
- Закрасили выпадающие списки под темную тему:
  - `option` и `select` получили безопасный контраст через `background`/`color`.
- Уточнили поведение Caddy для снятия браузерного prompt авторизации:
  - оставлен `header_down -WWW-Authenticate` для подавления системного basic-auth dialog при уже работающем JWT-сессии frontend.

### Что осталось до ручной финальной валидации

- Проверить в браузере на живом VPS отсутствие всплывающего basic-auth popup при нескольких сессиях.
- Финально проверить UX в collapsed sidebar/тяжёлом окне и при скролле после обновления мобильной шапки.
- После деплоя повторно проверить и зафиксировать список багов/улучшений для следующего спринта.

### Что проверили снаружи после этого апдейта

- `curl -k https://45.66.11.243:8443/healthz` — стабильные `200` (50/50 параллельных проверок).
- `/api/clients` и `/api/dashboard/summary` без токена — стабильные `401` без `WWW-Authenticate`.
- Параллельные проверок `/api/dashboard/summary` с валидным токеном — стабильные `200` (80/80).
- Через API логин `POST /api/auth/login` с `admin` возвращает `201` и валидный `accessToken`, `/api/dashboard/summary` с ним — `200`.

## Update 2026-03-31: SSH accessibility hardening in deploy scripts

- Проверка порта `22` с текущей среды показала, что порт сейчас доступен (TCP open), но вход без ключа/нужного auth-участка всё ещё может быть заблокирован провайдерской политикой.
- Внесены изменения:
  - `infra/scripts/ensure-ssh-access.sh` — отдельный guard, который:
    - принудительно держит `ufw` правило `allow 22/tcp` при активном UFW;
    - очищает ban-историю `fail2ban` для `sshd` (если установлен).
  - `infra/scripts/deploy.sh` — вызывает новый guard перед пересборкой/поднятием контейнеров.
  - `infra/scripts/bootstrap-server.sh` — добавил явное правило `ufw allow 22/tcp`.

## Update 2026-04-01: post-deploy hotfixes for Caddy and fail2ban guard

- Во время реального deploy на VPS обнаружился критичный баг:
  - `caddy` ушёл в restart loop из-за неверного использования `header_down` на верхнем уровне `Caddyfile`.
  - ошибка рантайма: `unrecognized directive: header_down`
- Исправление:
  - `header_down -WWW-Authenticate` перенесён внутрь каждого `reverse_proxy`, где он допустим синтаксически.
- Параллельно найден и исправлен баг в SSH-guard:
  - `fail2ban-client set sshd unban "*"` не поддерживается.
  - заменено на перечисление `Banned IP list` и `unbanip` по каждому адресу.
- После фикса требуется повторный deploy, чтобы вернуть `caddy` в healthy state и оставить рабочий SSH guard.

## Update 2026-04-01: VPS synced to latest hotfix and production recovered

- VPS repo в `/opt/server-vpn` обновлён до hotfix-коммита `c465d7f`.
- Повторный `bash infra/scripts/deploy.sh` завершился успешно:
  - `postgres`, `api`, `caddy`, `xray` перешли в `healthy/running`.
  - `caddy` перестал уходить в restart loop.
- Проверки после деплоя:
  - `https://45.66.11.243:8443/healthz` -> `200`
  - `https://45.66.11.243:8443/readyz` -> `200`
  - `https://45.66.11.243:8443/api/dashboard/summary` без токена -> `401`
  - в ответе больше нет `WWW-Authenticate`, значит browser basic-auth popup со стороны reverse proxy убран.
- SSH состояние после деплоя:
  - `sshd` слушает `0.0.0.0:22` и `[::]:22`
  - `ufw` содержит явные `ALLOW IN` для `22/tcp`
  - `fail2ban` для `sshd` остаётся активным и продолжает банить атакующие IP, но сам порт не закрыт firewall-ом.

## Update 2026-04-01: client status semantics aligned with live VPN state

- На странице клиентов логика статуса переведена с технического DB статуса на операторский live-смысл:
  - `Активен` = клиент реально подключён к VPN сейчас.
  - `Не активен` = клиент сейчас не подключён.
  - `Заблокирован` = доступ вручную выключен администратором.
- Ручная блокировка/разблокировка оставлена на базе `DISABLED`, чтобы не смешивать operator block с системными статусами `EXPIRED/BLOCKED`.
- В форме редактирования поле `Статус` заменено на `Доступ`:
  - `Разрешен`
  - `Заблокирован`
- В таблице клиентов убрана кнопка с тремя точками:
  - вместо неё теперь явная action-кнопка `Заблокировать / Разблокировать`.

## Update 2026-04-01: clients table layout stabilized

- Исправлен перекос таблицы клиентов на средних и узких desktop-width:
  - добавлен отдельный layout-модификатор `split-grid--clients`;
  - таблица клиентов получила собственные col widths и `min-width`;
  - для статусов, дат и action-кнопок отключены уродливые переносы.
- Action column теперь либо держит нормальную ширину, либо уходит в горизонтальный scroll внутри таблицы, но не ломает высоту/ширину строк.

## Update 2026-04-01: live online status stabilized across dashboard and clients list

- Удалён локальный служебный файл `server-vpn-web-access.txt`; такой же файл удалён и на VPS в `/opt/server-vpn`.
- Найден источник рассинхрона online-статуса:
  - дашборд считал raw `GetAllOnlineUsers` из Xray runtime;
  - список клиентов опирался на сохранённые `activeConnections` в `DailyClientUsage`;
  - при сбое per-user online stat Xray мог вернуть пользователя в `GetAllOnlineUsers`, но записать `0` в `activeConnections`, из-за чего дашборд показывал online, а список клиентов нет.
- Исправления в backend:
  - добавлен `resolveObservedActiveConnections(...)`, который считает клиента online, если Xray подтвердил присутствие пользователя даже при нулевом fallback-stat;
  - при каждом usage snapshot текущие `activeConnections` за текущий UTC-день сначала сбрасываются в `0`, чтобы offline-клиенты не “залипали”;
  - для клиентов с исторически последним `activeConnections > 0`, но без новой активности в текущем дне, создаётся/обновляется нулевая запись за текущий день, чтобы latest snapshot не тащился со вчера;
  - дашборд теперь считает `onlineNow` по тем же latest client snapshots, что и страница клиентов, а не по raw числу runtime users.
- Добавлены unit tests:
  - `apps/api/src/modules/xray/xray.helpers.spec.ts`
  - `apps/api/src/modules/dashboard/dashboard.service.spec.ts`

## Update 2026-04-01: navigation moved to on-demand drawer

- Левый sidebar больше не занимает постоянную колонку в desktop layout.
- Навигация теперь открывается по кнопке меню в topbar и отображается как overlay-drawer поверх контента.
- Основной рабочий центр получил всю доступную ширину, поэтому таблицы и split-layout больше не ужимаются из-за постоянной левой панели.
- Закрытие drawer работает:
  - по крестику внутри панели;
  - по клику в overlay;
  - по клавише `Esc`.

## Update 2026-04-01: fixed online-client detection for Xray runtime users

- Найдена точная причина, почему подключённый клиент не становился `Активен` даже спустя несколько минут:
  - Xray `GetAllOnlineUsers` на этой инсталляции возвращает не чистый `emailTag`, а stat name формата `user>>>admin-note-083384c6>>>online`.
  - backend сравнивал это значение как будто это уже `emailTag`, из-за чего:
    - `onlineUserSet.has(client.emailTag)` всегда возвращал `false`;
    - `loadOnlineIpCountMap(...)` строил некорректное имя online-stat на основе уже готового stat name;
    - `DailyClientUsage.activeConnections` записывался как `0`, несмотря на реальный live traffic.
- Исправление:
  - добавлены `parseOnlineUserStatName(...)` и `normalizeOnlineUserEmailTags(...)`;
  - `getRuntimeSummary()` и `captureUsageSnapshot()` теперь нормализуют ответ `GetAllOnlineUsers` до чистых `emailTag` перед дальнейшей обработкой.
- Подтверждение проблемы до фикса на проде:
  - `xray-access.log` фиксировал live traffic с `email: admin-note-083384c6`;
  - raw gRPC вызовы внутри `api` контейнера показывали:
    - `GetAllOnlineUsers` -> `user>>>admin-note-083384c6>>>online`
    - `GetStatsOnlineIpList(user>>>admin-note-083384c6>>>online)` -> IP `188.163.21.217`
    - `GetStatsOnline(user>>>admin-note-083384c6>>>online)` -> `1`
  - при этом в Postgres `DailyClientUsage.activeConnections` для `admin_note` оставался `0`.
- Добавлены unit tests для новой нормализации online-user identifiers:
  - `apps/api/src/modules/xray/xray.helpers.spec.ts`
- Развёртывание и проверка:
  - коммит `e01aa49` отправлен в `origin/main`;
  - из-за приватного HTTPS remote на VPS обновление выполнено через локальный `git bundle` с последующим `git fetch /tmp/server-vpn-main.bundle main` и `git reset --hard FETCH_HEAD`;
  - `bash infra/scripts/deploy.sh` на VPS завершился успешно, контейнеры `api`, `caddy`, `postgres`, `xray` остались `healthy/running`;
  - форсированный `XrayService.captureUsageSnapshot({ force: true, reason: 'manual-verification' })` на VPS вернул:
    - `onlineUsers: ["admin-note-083384c6"]`
    - живой traffic delta по `admin-note-083384c6`
  - Postgres после snapshot:
    - `admin_note | admin-note-083384c6 | active=1`
  - прямой вызов `ClientsService.list({ page: 1, pageSize: 20 })` внутри собранного `api` контейнера тоже вернул для `admin-note-083384c6`:
    - `activeConnections: 1`
    - `lastActivatedAt` и `lastSeenAt` обновлены текущим временем snapshot.

## Update 2026-04-01: guided restore flow completed and hardened

- Закрыт оставшийся restore gap между UI и host-side workflow:
  - раньше панель показывала restore preflight и команды, но destructive flow всё ещё был полуручным и местами опасным;
  - дополнительно обнаружили, что UI строил restore-команды через контейнерный `BACKUP_DIR=/var/backups/server-vpn`, тогда как `infra/scripts/restore.sh` запускается на хосте и требует host path.
- Backend / env:
  - добавлен `BACKUP_HOST_DIR` для корректного host-side absolute path к архивам;
  - `BackupsService` теперь сериализует оба пути:
    - `hostAbsolutePath`
    - `containerAbsolutePath`
  - `GET /api/backups/:backupId/restore-plan` теперь возвращает:
    - корректные host-side команды `dryRun` / `restore`
    - `guidance.restoreScope` (`FULL` / `DATABASE_ONLY`)
    - `guidance.hostPathConfigured`
    - `guidance.createsSafeguardBackup`
    - `commands.verification[]` c post-restore проверками (`docker compose ps`, `healthz`, `readyz`, `logs`)
- Web UI:
  - страница `Backups` теперь показывает и контейнерный, и host-side backup directory;
  - restore modal переведён из просто “списка команд” в guided flow:
    - host path и container path архива
    - restore scope
    - явный флаг автоматического safeguard backup
    - operator checklist с обязательными подтверждениями
    - destructive restore command блокируется до подтверждения checklist и наличия host path
    - отдельный блок post-restore verification commands с копированием bundle
- Host-side restore script:
  - `infra/scripts/restore.sh` получил безопасный safeguard step перед destructive restore;
  - по умолчанию перед перезаписью БД/конфига создаётся новый archive snapshot текущего состояния;
  - safeguard backup после завершения restore регистрируется в `BackupSnapshot` и `AuditLog` уже в восстановленной базе;
  - добавлен optional override `--skip-safeguard-backup` для ручного edge-case использования;
  - `bash -n infra/scripts/restore.sh` и `bash -n infra/scripts/deploy.sh` локально прошли без синтаксических ошибок.
- Deploy / production verification:
  - кодовые коммиты:
    - `6f1b9af` `feat: harden guided backup restore flow`
    - `73d1444` `fix: avoid jest-dom matcher typing in backups test`
  - VPS синхронизирован до `73d1444` через `git bundle`, затем выполнен `bash infra/scripts/deploy.sh`;
  - during deploy `deploy.sh` автоматически прописал:
    - `BACKUP_HOST_DIR=/opt/server-vpn/infra/backup/output`
  - после деплоя:
    - `api`, `caddy`, `postgres`, `xray` -> `healthy`
    - `https://127.0.0.1:8443/healthz` -> `200`
    - `https://127.0.0.1:8443/readyz` -> `200`
  - для проверки restore-plan был создан реальный контрольный backup на VPS:
    - `server-vpn-20260401-120301Z.tar.gz`
    - статус `READY`, checksum совпал
  - живой `BackupsService.getRestorePlan(...)` на VPS вернул:
    - host path `/opt/server-vpn/infra/backup/output/server-vpn-20260401-120301Z.tar.gz`
    - `guidance.restoreScope = FULL`
    - `guidance.hostPathConfigured = true`
    - `guidance.createsSafeguardBackup = true`
    - `preflight.canRestore = true`
    - post-restore verification commands присутствуют.

## Update 2026-04-01: dashboard analytics and clearer trends

- Следующий приоритет после restore доведён до рабочего MVP:
  - `DashboardService.summary()` теперь возвращает не только totals/runtime, но и исторический блок `trends`;
  - тренды считаются по `DailyClientUsage` за последние `14` дней без сторонней аналитической таблицы.
- Backend:
  - добавлены агрегаты для дашборда:
    - `trends.windowDays = 14`
    - `trends.comparisonWindowDays = 7`
    - `trends.buckets[]` c `date`, `totalTrafficBytes`, `activeClients`
    - `trends.comparisons`
      - `last7DaysTrafficBytes`
      - `previous7DaysTrafficBytes`
      - `trafficDeltaPercent`
      - `averageDailyTrafficBytes`
      - `busiestDayDate`
      - `busiestDayTrafficBytes`
      - `activeClientsToday`
      - `peakActiveClients`
  - пустые дни теперь явно добиваются нулевыми bucket entries, чтобы график не “рвался” и comparison window было стабильным.
- Web UI:
  - на дашборде добавлен новый секционный блок `Тренды нагрузки / Traffic and activity trends`;
  - внутри:
    - comparison cards по текущему и предыдущему 7-дневному окну;
    - средний день;
    - пиковый день;
    - сколько клиентов замечено онлайн сегодня;
    - ниже 14-дневный список с барами по трафику и количеством клиентов, замеченных онлайн в daily snapshot.
  - отображение собрано без chart library, на существующих `SectionCard`, `insight-card`, `history-row`, чтобы не раздувать стек.
- Типы и вспомогательные функции:
  - расширен `DashboardSummary` в web API types;
  - добавлен `formatDate(...)` для чистого date-only отображения дневных bucket rows.
- Тесты:
  - обновлён unit test `apps/api/src/modules/dashboard/dashboard.service.spec.ts`;
  - обновлён UI test `apps/web/src/features/dashboard/dashboard-page.spec.tsx`.
- Локальная верификация:
  - `git diff --check` прошёл;
  - полноценный local `npm`/`vitest` run на этой машине по-прежнему недоступен, потому что системных `node` / `npm` нет;
  - дополнительно попытались прогнать тесты через временный `node:22-alpine` контейнер, но локальный Docker daemon в момент проверки не был поднят (`docker.sock` недоступен).
- Ожидаемая прод-проверка после деплоя:
  - `docker compose ps`
  - `https://127.0.0.1:8443/healthz`
  - `https://127.0.0.1:8443/readyz`
  - авторизованный `GET /api/dashboard/summary` должен вернуть новый блок `trends` и не ломать старые totals/runtime.

## Update 2026-04-01: per-config device limits UX and unlimited mode

- По лимитам клиентов был найден и закрыт UI/UX разрыв:
  - runtime enforcement для `deviceLimit` / `ipLimit` в backend уже существовал;
  - но в web UI лимиты нельзя было задать при создании клиента;
  - а при редактировании пустое поле не снимало лимит обратно в режим `без ограничений`, потому что `PATCH` отправлял `undefined`, а не `null`.
- Что исправлено:
  - в форме `Create client` добавлены поля:
    - `Лимит устройств / Device limit`
    - `Лимит IP / IP limit`
  - пустое значение теперь явно документировано как режим `без ограничений`;
  - в `PATCH /api/clients/:id` web UI теперь отправляет `null`, если лимит очищен, и таким образом реально снимает ограничение;
  - в карточке выбранного клиента теперь видны текущие значения:
    - `Лимит устройств`
    - `Лимит IP`
    - либо `Без лимита`, если ограничение не задано.
- UX-пояснение, зафиксированное и в интерфейсе, и для будущей поддержки:
  - `IP limit` ограничивает число одновременных внешних IP для одного конфига;
  - в текущем Xray-based runtime это по-прежнему приближённый enforcement:
    - `deviceLimit` и `ipLimit` опираются на online endpoint / online IP телеметрию;
    - это не настоящий hardware fingerprint устройства.
- Тестовое покрытие обновлено:
  - добавлен UI test на создание клиента с лимитами;
  - добавлен UI test на очистку лимитов обратно в unlimited mode.

## Update 2026-04-01: clients table action button overflow fix

- Исправлен визуальный дефект на странице клиентов:
  - в строках таблицы кнопка `Заблокировать / Разблокировать` упиралась в правую границу и визуально обрезалась.
- Что изменено:
  - client split layout стал менее агрессивно забирать ширину под правую карточку;
  - action column таблицы получила больше рабочего места;
  - compact action button в таблице сделана уже по padding/font-size;
  - `QR + action` в ячейке переведены на более стабильную grid-композицию;
  - текст destructive action получил безопасный ellipsis fallback вместо жёсткого визуального обрезания.

## Update 2026-04-01: selected client row highlight

- Исправлен UX-дефект выбора клиента в таблице:
  - раньше выбранная строка почти не отличалась от остальных;
  - при работе со списком клиентов было визуально неочевидно, какой клиент сейчас открыт в правой карточке.
- Что добавлено:
  - явный selected-state для строки таблицы клиентов;
  - мягкая подсветка всей выбранной строки;
  - контрастный левый inset divider/accent на первой ячейке;
  - `aria-pressed` на кнопке выбора строки для более корректного accessibility-state и UI test coverage.

## Update 2026-04-01: right-side client panel responsive overlap fix

- Исправлен layout bug в правой карточке клиента:
  - при уменьшении ширины окна поля формы начинали наезжать друг на друга;
  - особенно это проявлялось на `datetime-local`, `select` и парных полях лимитов.
- Что сделано:
  - дочерним элементам `split-grid` задан `min-width: 0`, чтобы правая колонка реально могла сжиматься без layout overflow;
  - для полей формы в detail panel добавлен отдельный adaptive grid mode;
  - `input` и `select` в `login-form__field` теперь явно имеют `width: 100%` и `min-width: 0`, чтобы не выталкивать соседнюю колонку;
  - detail-side `field-grid` теперь перестраивается через `auto-fit/minmax(...)` вместо жёсткой двухколоночной сетки внутри узкой панели.

## Update 2026-04-01: guided one-command install helper and docs cleanup

- В основной репозиторий добавлен root-level [install.sh](/Users/pc/server-VPN/install.sh) как guided installer для fresh VPS.
- Скрипт:
  - переисполняется через `sudo`, если это нужно;
  - запускает `infra/scripts/bootstrap-server.sh`;
  - создаёт `.env` из `.env.example`, если файла ещё нет;
  - генерирует недостающие `POSTGRES_PASSWORD`, `JWT_*`, `TOTP_ENCRYPTION_SECRET`, `XRAY_SHORT_IDS` и `REALITY` keypair;
  - нормализует panel URLs под указанный host/IP;
  - затем выполняет `infra/scripts/deploy.sh`.
- В [infra/scripts/bootstrap-server.sh](/Users/pc/server-VPN/infra/scripts/bootstrap-server.sh) добавлены `git` и `openssl` в bootstrap install list, а финальная подсказка теперь указывает на `install.sh`.
- В `.env.example`, `README`, `DEPLOY`, `docs/ru/DEPLOY`, `ADMIN_GUIDE`, `TROUBLESHOOTING` и русских версиях вычищены stale references на неиспользуемые `PANEL_TLS_*`, `PANEL_BASIC_AUTH_*` и старый путь через `docker compose up -d --build`.
- Следующий шаг после этого изменения: синхронизировать public repo так, чтобы код helper-а и install docs появились и там, но public-only README/asset layer остался нетронут.

## Update 2026-04-01: README install examples made more readable

- В `README` основного и public репозитория добавлены короткие ready-to-copy примеры для:
  - IP-only установки через `install.sh --host <public-ip>`;
  - non-interactive установки с `--host`, `--admin-*` и `--non-interactive`.
- Отдельно зафиксирована operator-подсказка:
  - для ручных запусков лучше не передавать `--admin-password` в shell history;
  - этот флаг нужен в первую очередь для automation/provisioning.
