# Диагностика

## Панель Открывается С Предупреждением О Сертификате

В `PANEL_TLS_MODE=ip` панель работает на `8443` с внутренним сертификатом Caddy. Браузер будет показывать предупреждение.

В `PANEL_TLS_MODE=domain` панель работает на `https://DOMAIN` с Let's Encrypt через HAProxy SNI routing. Если сертификат не выпускается, проверь что домен указывает на VPS и что `80/tcp` и `443/tcp` открыты.

## Браузер Показывает Старую Версию UI После Деплоя

Сделай полный reload страницы или открой панель в новой вкладке, чтобы сбросить закешированные frontend assets после обновления.

## Админ Не Может Войти

- проверь пароль
- проверь, не требуется ли `TOTP 2FA`
- проверь rate limiting на auth endpoints
- проверь логи `API` и `Caddy`

## Клиент Не Подключается

- убедись, что используемый клиент поддерживает `REALITY`
- проверь, что `XRAY_REALITY_PUBLIC_KEY`, `shortId` и `serverName` совпадают с выдаваемой подпиской
- проверь, что клиент не истёк и не заблокирован политикой лимитов
- в domain режиме старые ссылки на IP продолжают работать через default route HAProxy, если REALITY SNI в них не менялся

## Backup / Restore

- перед restore всегда запускай `--dry-run`
- не выполняй restore без абсолютного пути к архиву
- после restore проверь `healthz`, `readyz` и live sync с `Xray`
